const { Ingrediente, TipoIngrediente } = require('./schema.js');
const IngredienteMutation = require('./mutation.js');

module.exports = {
    Ingrediente,
    TipoIngrediente,
    IngredienteMutation,
};
